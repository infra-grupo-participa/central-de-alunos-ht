// Arquivo de inicializacao para Hostinger (Node app / Passenger).
// Define este arquivo como "Application startup file" no painel.
// Sobe a API Fastify, que tambem serve o build do client em producao.
import './server/src/index.js';
